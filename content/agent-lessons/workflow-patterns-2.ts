import type { Lesson } from "@/lib/types";

const lesson: Lesson = {
  slug: "workflow-patterns-2",
  title: "ワークフローパターン②",
  subtitle: "並列化、オーケストレーター-ワーカー — 複雑なタスクを分割統治する",
  sections: [
    {
      id: "intro",
      title: "はじめに",
      blocks: [
        {
          type: "text",
          content: `前回はプロンプトチェーンとルーティングを学びました。どちらも「直列的」なパターンでしたが、今回は**並行処理**と**動的な分割**を扱う2つのパターンを学びます：

1. **並列化（Parallelization）** — 同じタスクを複数のLLMに同時に処理させる
2. **オーケストレーター-ワーカー** — 中央のLLMがタスクを分解し、ワーカーに割り振る

これらはより複雑ですが、特定のユースケースで大きな効果を発揮します。`,
        },
      ],
    },
    {
      id: "parallelization",
      title: "並列化",
      blocks: [
        {
          type: "text",
          content: `**並列化（Parallelization）**は、タスクを複数のLLMに同時に処理させるパターンです。2つのバリエーションがあります：

**セクショニング（Sectioning）**
- タスクを独立したサブタスクに分割し、並行処理する
- 例：文書のセクション別要約、多角的なコードレビュー

**投票（Voting）**
- 同じタスクを複数のLLMに独立して実行させ、結果を集約する
- 例：コンテンツの安全性チェック（複数モデルの多数決）

**いつ使うか：**
- サブタスクが独立しており、並行処理可能なとき
- 信頼性を高めたいとき（複数の視点で検証）
- レイテンシを下げたいとき（直列より並列の方が速い）

下のデモで、並列化の動きを確認してみましょう。`,
        },
        {
          type: "interactive",
          id: "parallel-demo",
        },
      ],
    },
    {
      id: "orchestrator-worker",
      title: "オーケストレーター-ワーカー",
      blocks: [
        {
          type: "text",
          content: `**オーケストレーター-ワーカー（Orchestrator-Workers）**は、中央のLLM（オーケストレーター）がタスクを動的に分解し、ワーカーLLMに割り振り、結果を統合するパターンです。

並列化との違いは、**サブタスクが事前に決まっていない**ことです。オーケストレーターが入力を見て、必要なサブタスクを判断します。

**いつ使うか：**
- 必要なサブタスクが入力によって変わるとき
- タスクの分解に判断が必要なとき

**具体例：**
- コーディングタスク：オーケストレーターが変更すべきファイルを特定し、各ファイルの修正をワーカーに割り振る
- リサーチ：メインの質問を複数のサブ質問に分解し、各ワーカーが調査して結果を統合
- 文書作成：構成をオーケストレーターが決め、各セクションをワーカーが執筆

下のデモで、オーケストレーター-ワーカーの流れをステップごとに見てみましょう。`,
        },
        {
          type: "interactive",
          id: "orchestrator-demo",
        },
      ],
    },
    {
      id: "summary",
      title: "まとめ",
      blocks: [
        {
          type: "text",
          content: `このレッスンのポイント：

- **並列化**：同じタスクを複数のLLMに同時に処理させ、結果を集約する
  - **セクショニング**：独立したサブタスクに分けて並行処理
  - **投票**：同じタスクを複数実行し、多数決や合議で判定
- **オーケストレーター-ワーカー**：中央のLLMがタスクを動的に分解し、ワーカーに割り振る
- ワークフローパターンの使い分け：
  - 手順が固定 → **プロンプトチェーン**
  - 種類で分岐 → **ルーティング**
  - 独立して並行 → **並列化**
  - 動的に分解 → **オーケストレーター-ワーカー**

次のレッスンでは、最後のワークフローパターン「エバリュエーター-オプティマイザー」を学びます。`,
        },
        {
          type: "quiz",
          data: {
            question:
              "「コーディングタスクで、変更すべきファイルをLLMが特定し、各ファイルの修正を別のLLMに振り分ける」パターンは？",
            options: [
              "プロンプトチェーン",
              "ルーティング",
              "並列化（投票）",
              "オーケストレーター-ワーカー",
            ],
            answer: 3,
            explanation:
              "サブタスクが入力によって動的に決まり（どのファイルを変更するかは事前に分からない）、中央のLLMが分解と統合を行うのは「オーケストレーター-ワーカー」パターンです。並列化との違いは、サブタスクが事前に決まっていない点です。",
          },
        },
      ],
    },
  ],
};

export default lesson;
