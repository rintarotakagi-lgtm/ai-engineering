import type { Lesson } from "@/lib/types";

const lesson: Lesson = {
  slug: "agent-loop",
  title: "エージェントループ",
  subtitle: "エージェントの心臓部 — Observe → Think → Act を繰り返す仕組み",
  sections: [
    {
      id: "heart",
      title: "エージェントの心臓部",
      blocks: [
        {
          type: "text",
          content: `前のレッスンで、エージェントは「LLM + ツール + ループ」だと学びました。このレッスンでは、その**ループ**の中身を詳しく見ていきます。

エージェントの核心は、**ReAct（Reasoning + Acting）ループ**と呼ばれるサイクルです。これは人間が仕事をするときの思考プロセスとほぼ同じです：

1. **Observe（観察）** — 現在の状況・情報を確認する
2. **Think（思考）** — 次に何をすべきか考える
3. **Act（行動）** — ツールを使って実際にアクションを起こす
4. **繰り返し** — 結果を観察し、タスクが完了するまで続ける

このサイクルをプログラムとして実装したものが**エージェントループ**です。下のデモで、具体的な動きを見てみましょう。`,
        },
        {
          type: "interactive",
          id: "react-demo",
        },
        {
          type: "text",
          content: `エージェントループの実装はシンプルです。擬似コードで書くとこうなります：

\`\`\`
while True:
    # 1. プロンプトを組み立てる（システム指示 + 会話履歴 + ツール結果）
    prompt = assemble_prompt(messages, tool_results)

    # 2. LLMを呼ぶ
    response = call_llm(prompt)

    # 3. レスポンスを解析
    if response.has_tool_call:
        # ツール呼び出しがあれば実行
        result = execute_tool(response.tool_call)
        tool_results.append(result)
    else:
        # ツール呼び出しがなければ完了
        return response.text
\`\`\`

ポイントは**ループの終了条件**です。LLMが「もうツールを使わなくていい」と判断したとき（＝テキストだけを返したとき）、ループが終了します。`,
        },
      ],
    },
    {
      id: "stop-conditions",
      title: "停止条件",
      blocks: [
        {
          type: "text",
          content: `エージェントループは「タスク完了」で止まるのが理想ですが、実際にはそれ以外にも**停止条件**が必要です。無限ループを防ぐためのセーフティネットです。

### 主な停止条件

| 条件 | 説明 | 例 |
|------|------|-----|
| **タスク完了** | LLMがツールを呼ばずにテキストを返す | 「要約が完了しました」 |
| **最大反復回数** | ループの上限に達した | 25回のツール呼び出しで打ち切り |
| **エラー** | ツール実行でエラーが発生し、回復不能 | APIキーが無効 |
| **ユーザー中断** | ユーザーが手動で停止 | Ctrl+C |
| **コスト上限** | トークン消費が上限に達した | $10を超えたら停止 |

### 最大反復回数の重要性

最大反復回数の設定は特に重要です。設定しないと、エージェントが同じツールを何度も呼び続ける**無限ループ**に陥ることがあります。

例えば「ファイルを探して」というタスクで、目的のファイルが存在しない場合、エージェントは永遠に探し続けてしまう可能性があります。最大反復回数を設定しておけば、一定回数で「見つかりませんでした」と報告して止まります。`,
        },
      ],
    },
    {
      id: "trace",
      title: "エージェントの実行トレース",
      blocks: [
        {
          type: "text",
          content: `エージェントが実際にタスクを実行するとき、内部では何が起きているのでしょうか？**実行トレース（Execution Trace）**を見てみましょう。

実行トレースとは、エージェントがタスクを完了するまでの全ステップの記録です。デバッグや改善に不可欠で、エージェントが「何を考え、何をしたか」が分かります。

下のデモでは、「プロジェクトのREADME.mdを見つけて内容を要約して」というタスクの実行トレースを確認できます。各ステップをクリックして詳細を見てみましょう。`,
        },
        {
          type: "interactive",
          id: "agent-trace-demo",
        },
        {
          type: "text",
          content: `トレースを読むときのポイント：

- **思考（Think）ステップ**では、エージェントが次のアクションを選ぶ理由が分かる
- **行動（Act）ステップ**では、具体的なツール呼び出しとそのパラメータが分かる
- **観察（Observe）ステップ**では、ツールからの返却値が分かる
- ステップ数が多すぎる場合、ツール設計やプロンプトの改善が必要なサイン`,
        },
      ],
    },
    {
      id: "summary",
      title: "まとめ",
      blocks: [
        {
          type: "text",
          content: `このレッスンのポイント：

- エージェントの核心は**ReActループ**（Observe → Think → Act → 繰り返し）
- ループは**LLMがツールを呼ばなくなった時**に終了する
- **停止条件**（最大反復回数・コスト上限など）で無限ループを防ぐ
- **実行トレース**でエージェントの思考と行動を可視化できる
- 次のレッスンでは、エージェントのパフォーマンスを左右する**ツール設計**を学びます`,
        },
        {
          type: "quiz",
          data: {
            question:
              "エージェントループが停止する条件として、最も基本的なものはどれ？",
            options: [
              "一定時間が経過した",
              "LLMがツール呼び出しをせずテキストだけを返した",
              "全てのツールを1回ずつ使い終わった",
              "ユーザーが「完了」と入力した",
            ],
            answer: 1,
            explanation:
              "エージェントループの最も基本的な停止条件は「LLMがツール呼び出しをせずテキストだけを返したとき」です。これはLLMが「もうツールを使う必要がない＝タスクが完了した」と判断したことを意味します。",
          },
        },
      ],
    },
  ],
};

export default lesson;
