import type { Lesson } from "@/lib/types";

const lesson: Lesson = {
  slug: "multi-agent",
  title: "マルチエージェント",
  subtitle: "サブエージェントとMCPで能力を拡張する",
  sections: [
    {
      id: "subagent",
      title: "サブエージェント",
      blocks: [
        {
          type: "text",
          content: `1つのエージェントで全てをこなそうとすると、システムプロンプトが肥大化し、ツールが増えすぎて、パフォーマンスが低下します。

これは人間の組織と同じです。1人で全てやるより、専門家チームで分担した方が効率的です。

**サブエージェント**とは、メインエージェントが特定のタスクのために起動する**専門特化型のエージェント**です。

### サブエージェントの仕組み

1. メインエージェントが複雑なタスクを受け取る
2. タスクの一部を**専門的な指示**と共にサブエージェントに委任
3. サブエージェントが独自のツールセットで作業を実行
4. 結果をメインエージェントに報告
5. メインエージェントが結果を統合

### 使いどころ

- **コードレビュー**: メインが概要を把握し、セキュリティ専門・パフォーマンス専門のサブエージェントが詳細チェック
- **リサーチ**: 複数の情報源を並行で調べる
- **翻訳＋校正**: 翻訳エージェントと校正エージェントを分離`,
        },
        {
          type: "interactive",
          id: "subagent-demo",
        },
      ],
    },
    {
      id: "mcp",
      title: "MCP（Model Context Protocol）",
      blocks: [
        {
          type: "text",
          content: `**MCP（Model Context Protocol）**は、LLMと外部サービスを接続するための標準プロトコルです。Anthropicが策定し、オープンスタンダードとして公開しています。

### USBポートのようなもの

MCPを理解する最も簡単な方法は、**USBポート**の例えです：

- USBが登場する前、周辺機器（プリンタ・マウス・キーボード）はそれぞれ専用の接続端子が必要だった
- USBが統一規格を作ったことで、どんな機器もUSBポート1つで接続できるようになった
- MCPも同じ。LLMと外部サービス（ブラウザ・DB・Slack・GitHubなど）の接続を**1つの標準プロトコル**で統一する

### MCPサーバーの構造

\`\`\`
LLM（クライアント）
  ├── MCP Server: ブラウザ操作 → ページ読み取り、フォーム入力
  ├── MCP Server: データベース → SQLクエリ、データ取得
  ├── MCP Server: Slack → メッセージ送信、チャンネル検索
  └── MCP Server: GitHub → Issue作成、PR作成
\`\`\`

各MCPサーバーは独立して動作し、LLMに対して「使えるツール」のリストを公開します。LLMはそのツールを通常のツール呼び出しと同じ方法で使えます。`,
        },
        {
          type: "interactive",
          id: "mcp-demo",
        },
      ],
    },
    {
      id: "orchestration",
      title: "オーケストレーション戦略",
      blocks: [
        {
          type: "text",
          content: `複数のサブエージェントをどう連携させるか？主に2つの戦略があります。

### 1. 逐次実行（Sequential）

サブエージェントを**順番に**実行します。前のサブエージェントの結果を次のサブエージェントに渡します。

\`\`\`
メインエージェント
  → サブエージェントA（リサーチ）→ 結果A
  → サブエージェントB（結果Aを元に分析）→ 結果B
  → サブエージェントC（結果Bを元にレポート作成）→ 最終出力
\`\`\`

**使うタイミング:** 各ステップが前のステップの出力に依存する場合

### 2. 並列実行（Parallel）

サブエージェントを**同時に**実行します。独立したタスクを並行処理できます。

\`\`\`
メインエージェント
  → サブエージェントA（市場調査）   ─┐
  → サブエージェントB（競合分析）   ─┼→ 結果を統合 → 最終レポート
  → サブエージェントC（技術トレンド）─┘
\`\`\`

**使うタイミング:** 各タスクが独立していて、互いの結果を必要としない場合

### 選択の指針

| 条件 | 戦略 |
|------|------|
| タスク間に依存関係がある | 逐次実行 |
| タスクが独立している | 並列実行 |
| 速度が重要 | 並列実行 |
| 結果の品質が重要 | 逐次実行（各ステップで検証可能） |
| コスト制約がある | 逐次実行（途中で中断可能） |`,
        },
      ],
    },
    {
      id: "summary",
      title: "まとめ",
      blocks: [
        {
          type: "text",
          content: `このレッスンのポイント：

- **サブエージェント**は専門特化型のエージェント。メインが委任し、結果を統合する
- **MCP**はLLMと外部サービスを接続する標準プロトコル（USBポートのような統一規格）
- オーケストレーションは**逐次**（依存関係あり）と**並列**（独立タスク）の2戦略
- 1つの万能エージェントより、専門エージェントの組み合わせの方が実用的
- 次のレッスンでは、エージェントを本番運用するための**ハーネス設計**を学びます`,
        },
        {
          type: "quiz",
          data: {
            question:
              "MCPの役割として最も適切な説明はどれ？",
            options: [
              "LLM同士を接続して会話させるプロトコル",
              "LLMと外部サービスをツール呼び出しで接続する標準プロトコル",
              "エージェントのエラーを自動修復するプロトコル",
              "エージェントの実行ログを記録するプロトコル",
            ],
            answer: 1,
            explanation:
              "MCP（Model Context Protocol）は、LLMとブラウザ・DB・Slackなどの外部サービスを、標準化されたツール呼び出しで接続するためのプロトコルです。USBポートのような統一規格として機能します。",
          },
        },
      ],
    },
  ],
};

export default lesson;
