import type { Lesson } from "@/lib/types";

const lesson7: Lesson = {
  slug: "cnn",
  title: "CNN（畳み込みニューラルネットワーク）",
  subtitle:
    "畳み込み、特徴マップ、プーリング、CNN全体構造 — 画像認識を支えるアーキテクチャの基礎",
  sections: [
    {
      id: "intro",
      title: "はじめに",
      blocks: [
        {
          type: "text",
          content: `前回のレッスンで、多層ニューラルネットワーク（全結合層）を学びました。全結合層は万能近似定理が保証するように、原理的にはあらゆる関数を近似できます。

しかし、**画像**を扱おうとすると深刻な問題が起きます。

例えば $28 \\times 28$ ピクセルのグレースケール画像（MNISTの手書き数字）を全結合層に入力する場合を考えましょう：

- 入力ノード数: $28 \\times 28 = 784$
- 最初の隠れ層が256ノードなら、パラメータ数は $784 \\times 256 = 200{,}704$

まだ管理可能ですが、カラー画像 $224 \\times 224 \\times 3$（ImageNetの標準サイズ）では：

$$224 \\times 224 \\times 3 = 150{,}528 \\text{ 入力}$$

隠れ層1024ノードで **約1.5億パラメータ** — たった1層で。これは現実的ではありません。

さらに根本的な問題があります。全結合層は**ピクセルの位置関係を無視**します。画像の左上にある猫も右下にある猫も、全結合層にとっては完全に異なる入力パターンです。

CNNは、この2つの問題を解決するために生まれました：

- **パラメータの爆発** → 畳み込みで局所的な接続にする（パラメータ共有）
- **位置不変性の欠如** → 同じカーネルを画像全体にスライドさせる（平行移動不変性）`,
        },
      ],
    },
    {
      id: "convolution",
      title: "畳み込み演算",
      blocks: [
        {
          type: "text",
          content: `**畳み込み（convolution）**は、小さな**カーネル（フィルタ）**を入力画像の上でスライドさせ、各位置で要素ごとの積の合計を計算する操作です。

$3 \\times 3$ カーネル $K$ を入力画像 $I$ に適用する場合：

$$\\text{Output}(i, j) = \\sum_{m=0}^{2} \\sum_{n=0}^{2} K(m, n) \\cdot I(i+m, j+n)$$

この操作のポイント：

1. **局所性** — カーネルサイズ分の領域だけを見る（$3 \\times 3$ なら9ピクセル）
2. **パラメータ共有** — 同じカーネルを画像全体で使い回す
3. **平行移動不変性** — 特徴がどこにあっても同じカーネルで検出できる

$3 \\times 3$ カーネルなら、パラメータはたった**9個**。全結合層の数百万パラメータと比べて圧倒的に少ないです。`,
        },
        {
          type: "text",
          content: `下のデモで、畳み込みの動きを確認しましょう。$5 \\times 5$ の入力グリッドに $3 \\times 3$ カーネルをスライドさせます。

プリセットを切り替えると、異なるカーネルがどんな特徴を検出するか体感できます：

- **エッジ検出**: 値の変化（境界）を強調
- **ぼかし**: 周囲の平均を取って滑らかにする
- **シャープ化**: 元の値を強調しつつ周囲との差を際立たせる`,
        },
        {
          type: "interactive",
          id: "convolution-demo",
        },
        {
          type: "quiz",
          data: {
            question:
              "5×5 の入力画像に 3×3 カーネルをストライド1・パディングなしで畳み込むと、出力サイズは？",
            options: ["5×5", "4×4", "3×3", "2×2"],
            answer: 2,
            explanation:
              "出力サイズは (入力サイズ − カーネルサイズ + 1) = 5 − 3 + 1 = 3 なので 3×3 になります。パディングを追加すれば出力サイズを保つことも可能です。",
          },
        },
      ],
    },
    {
      id: "feature-maps",
      title: "特徴マップ",
      blocks: [
        {
          type: "text",
          content: `1つのカーネルは1種類の特徴しか検出できません。エッジ検出カーネルは縦エッジを見つけますが、横エッジは見逃します。

そこで、**複数のカーネルを並列に適用**します。$N$ 個のカーネルを使えば、$N$ 枚の**特徴マップ（feature map）**が得られます。

$$\\text{出力チャンネル数} = \\text{カーネルの数}$$

実際のCNNでは、最初の畳み込み層で32〜64個のカーネルを使うのが一般的です。各カーネルが異なるパターンを検出します：

- あるカーネル → 横エッジ
- あるカーネル → 縦エッジ
- あるカーネル → 斜めエッジ
- あるカーネル → 角・コーナー

層が深くなるほど、検出する特徴は**抽象的**になります：
- 第1層: エッジ、テクスチャ
- 第2層: パーツ（目、鼻、タイヤ）
- 第3層: オブジェクト全体（顔、車）`,
        },
        {
          type: "interactive",
          id: "feature-map-demo",
        },
      ],
    },
    {
      id: "pooling",
      title: "プーリング",
      blocks: [
        {
          type: "text",
          content: `畳み込みで特徴マップを得たら、次に**プーリング（pooling）**で空間サイズを縮小します。

プーリングの目的：
1. **計算量の削減** — 特徴マップのサイズを小さくする
2. **微小な位置ずれへのロバスト性** — 特徴の正確な位置よりも「存在するかどうか」を重視
3. **受容野の拡大** — 後続の層がより広い範囲を「見る」ことができる

最も一般的な2つの方法：

**Maxプーリング:**
$$\\text{MaxPool}(\\text{region}) = \\max(\\text{region内の値})$$

領域内の**最大値**を取る。最も強い特徴の反応を残す。

**Averageプーリング:**
$$\\text{AvgPool}(\\text{region}) = \\frac{1}{|\\text{region}|} \\sum(\\text{region内の値})$$

領域内の**平均値**を取る。全体的な特徴の強さを要約する。

一般的には $2 \\times 2$ のウィンドウをストライド2で適用し、特徴マップのサイズを**縦横それぞれ半分**にします。`,
        },
        {
          type: "interactive",
          id: "pooling-demo",
        },
        {
          type: "quiz",
          data: {
            question:
              "6×6 の特徴マップに 2×2 の Max プーリング（ストライド2）を適用すると、出力サイズは？",
            options: ["6×6", "5×5", "3×3", "2×2"],
            answer: 2,
            explanation:
              "2×2 プーリングをストライド2で適用すると、縦横それぞれ半分になります。6÷2 = 3 なので出力は 3×3 です。",
          },
        },
      ],
    },
    {
      id: "cnn-architecture",
      title: "CNN全体像",
      blocks: [
        {
          type: "text",
          content: `ここまでの要素を組み合わせると、CNNの典型的な構造が見えてきます：

$$\\text{入力} \\to [\\text{Conv} \\to \\text{ReLU} \\to \\text{Pool}] \\times N \\to \\text{Flatten} \\to \\text{FC} \\to \\text{出力}$$

各段階の役割：

1. **Conv（畳み込み）** — 局所的なパターンを検出
2. **ReLU（活性化関数）** — 非線形性を導入（$\\max(0, x)$）
3. **Pool（プーリング）** — 空間サイズを縮小
4. **Flatten（平坦化）** — 2D特徴マップを1Dベクトルに変換
5. **FC（全結合層）** — 抽出した特徴をもとに最終的な分類

層を重ねるほど：
- **空間サイズ**は小さくなる（プーリングで縮小）
- **チャンネル数**は増える（より多くの特徴を検出）
- **特徴の抽象度**が上がる（低レベル→高レベル）

例えばLeNet-5（1998年、手書き数字認識）：

| 層 | 出力サイズ |
|---|---|
| 入力 | $32 \\times 32 \\times 1$ |
| Conv1 (6 filters, 5×5) | $28 \\times 28 \\times 6$ |
| Pool1 (2×2) | $14 \\times 14 \\times 6$ |
| Conv2 (16 filters, 5×5) | $10 \\times 10 \\times 16$ |
| Pool2 (2×2) | $5 \\times 5 \\times 16$ |
| Flatten | $400$ |
| FC → 出力 | $10$ （数字0〜9） |`,
        },
        {
          type: "interactive",
          id: "cnn-architecture",
        },
      ],
    },
    {
      id: "summary",
      title: "まとめ",
      blocks: [
        {
          type: "text",
          content: `このレッスンでは、CNNの基本要素を学びました：

1. **畳み込み** — 小さなカーネルで局所パターンを検出。パラメータ共有により効率的
2. **特徴マップ** — 複数カーネルで多様な特徴を並列に抽出
3. **プーリング** — 空間サイズの縮小と位置不変性の獲得
4. **CNN全体構造** — Conv→ReLU→Poolの繰り返し＋全結合層

CNNが画像認識で強力な理由は、**局所性・パラメータ共有・階層的な特徴抽出**という3つの帰納的バイアスが、画像データの性質と完璧にマッチしているからです。

次のレッスンでは、CNNの学習方法である**誤差逆伝播法（バックプロパゲーション）**と、有名なCNNアーキテクチャ（AlexNet, VGG, ResNet）を見ていきます。`,
        },
        {
          type: "quiz",
          data: {
            question:
              "CNNが全結合ネットワークより画像認識に優れている主な理由として、最も適切なものは？",
            options: [
              "活性化関数の種類が多いから",
              "局所的な接続とパラメータ共有により、空間構造を効率的に捉えられるから",
              "学習率を自動調整できるから",
              "全結合層を使わないから",
            ],
            answer: 1,
            explanation:
              "CNNの核心は「局所性」と「パラメータ共有」です。カーネルが局所的なパターンを検出し、同じカーネルを画像全体で使い回すことで、少ないパラメータで空間構造を効率的に学習できます。なお、CNN の最終段には全結合層も使われます。",
          },
        },
      ],
    },
  ],
};

export default lesson7;
