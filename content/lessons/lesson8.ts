import type { Lesson } from "@/lib/types";

const lesson8: Lesson = {
  slug: "rnn-lstm",
  title: "RNN・LSTM",
  subtitle:
    "系列データを扱うリカレントニューラルネットワーク — 隠れ状態、BPTT、勾配消失、そしてLSTMによる解決",
  sections: [
    {
      id: "intro",
      title: "はじめに",
      blocks: [
        {
          type: "text",
          content: `ここまでのレッスンでは、入力が**固定長のベクトル**であることを前提にしてきました。しかし現実には、**長さが可変で順序に意味がある**データが溢れています：

- **自然言語**: 「猫が魚を食べた」— 単語の順序が意味を決める
- **時系列**: 株価、気温、センサーデータ — 過去の値が未来に影響する
- **音声**: 波形の時間的パターンに意味がある
- **動画**: フレームの時間的つながりが重要

通常のニューラルネットワーク（MLP）では、入力間の**時間的な依存関係**を扱えません。各入力を独立に処理するため、「3つ前の単語が何だったか」を覚えていられないのです。

この問題を解決するのが**リカレントニューラルネットワーク（RNN: Recurrent Neural Network）**です。

キーワード：
- **RNN** — 隠れ状態で系列の「記憶」を保持
- **BPTT** — 時間を通じた誤差逆伝播
- **勾配消失問題** — 長い系列での学習の壁
- **LSTM** — ゲート機構による長期記憶の実現`,
        },
      ],
    },
    {
      id: "rnn-structure",
      title: "RNNの構造",
      blocks: [
        {
          type: "text",
          content: `**RNN** の核心は「**隠れ状態（hidden state）**」という内部メモリです。各タイムステップで、RNNは2つの入力を受け取ります：

1. 現在の入力 $x_t$
2. 前のタイムステップの隠れ状態 $h_{t-1}$

そしてこの2つから、新しい隠れ状態 $h_t$ を計算します：

$$h_t = \\tanh(W_{hh} \\cdot h_{t-1} + W_{xh} \\cdot x_t + b_h)$$

$$y_t = W_{hy} \\cdot h_t + b_y$$

ここで重要なのは、**すべてのタイムステップで同じ重み $W_{hh}, W_{xh}, W_{hy}$ を共有する**ことです。これを**重み共有（weight sharing）**と呼びます。

直感的に言えば、RNNは「同じ処理回路に、時間の流れに沿ってデータを次々と入力する装置」です。隠れ状態 $h_t$ は、過去のすべての入力の情報を（圧縮して）保持する「記憶」として機能します。`,
        },
        {
          type: "text",
          content: `下のデモでは、RNNセルを**折りたたみ表示**と**展開表示**で切り替えられます。

- **折りたたみ**: 1つのセルが自己ループで隠れ状態を保持（概念図）
- **展開**: 時間軸に沿って同じセルをコピーし、隠れ状態の流れを可視化

「隠れ状態の伝播」ボタンで、$h_t$ が時間に沿って流れていく様子をアニメーションで確認してみましょう。`,
        },
        {
          type: "interactive",
          id: "rnn-unrolled",
        },
        {
          type: "quiz",
          data: {
            question:
              "RNNで「重み共有」を行う理由として最も適切なのはどれ？",
            options: [
              "計算速度を上げるため",
              "任意の長さの系列を同じモデルで処理でき、パラメータ数を抑えられるため",
              "過学習を完全に防ぐため",
              "勾配消失を防ぐため",
            ],
            answer: 1,
            explanation:
              "重み共有により、系列の長さに関係なく同じパラメータセットで処理できます。長さ10でも100でも同じモデルが使えるのです。パラメータ数も系列長に依存せず一定に保てます。",
          },
        },
      ],
    },
    {
      id: "bptt",
      title: "RNNの学習：BPTT",
      blocks: [
        {
          type: "text",
          content: `RNNの学習には**BPTT（Backpropagation Through Time / 時間を通じた誤差逆伝播法）**を使います。

基本的なアイデアは：
1. RNNを時間方向に**展開**し、通常の多層ネットワークとみなす
2. 展開したネットワークに対して、通常の誤差逆伝播を適用する

損失関数 $L$ が最終タイムステップ $T$ で定義されているとすると、重み $W_{hh}$ に関する勾配は連鎖律を繰り返し適用して求めます：

$$\\frac{\\partial L}{\\partial W_{hh}} = \\sum_{t=1}^{T} \\frac{\\partial L}{\\partial h_T} \\cdot \\prod_{k=t+1}^{T} \\frac{\\partial h_k}{\\partial h_{k-1}} \\cdot \\frac{\\partial h_t}{\\partial W_{hh}}$$

ここで注目すべきは、$\\prod_{k=t+1}^{T} \\frac{\\partial h_k}{\\partial h_{k-1}}$ の部分 — **隠れ状態のヤコビアンの積**が時間ステップ分だけ累積することです。

これが後述する「勾配消失・爆発問題」の原因になります。`,
        },
        {
          type: "text",
          content: `下のデモで、勾配がタイムステップを逆方向に伝播していく様子を確認しましょう。**勾配係数**（各ステップで勾配が掛け算される比率）を変えてみてください：

- **< 1**: 勾配が指数的に減衰 → 勾配消失
- **= 1**: 理想的だが実際には維持が困難
- **> 1**: 勾配が指数的に増大 → 勾配爆発`,
        },
        {
          type: "interactive",
          id: "bptt-demo",
        },
      ],
    },
    {
      id: "vanishing-gradient",
      title: "勾配消失問題（再訪）",
      blocks: [
        {
          type: "text",
          content: `Lesson 5で活性化関数の文脈で「勾配消失」に触れましたが、RNNではこの問題が**より深刻**です。

通常のMLPでは「層の深さ」が問題でしたが、RNNでは「**系列の長さ**」が問題になります。100ステップの系列では、100回の行列積が発生します。

$$\\prod_{k=t+1}^{T} \\frac{\\partial h_k}{\\partial h_{k-1}} = \\prod_{k=t+1}^{T} W_{hh}^\\top \\cdot \\text{diag}(1 - h_k^2)$$

$\\tanh$ の導関数 $(1 - h_k^2)$ は常に $[0, 1]$ の範囲にあるため、$W_{hh}$ の最大特異値が1未満なら、この積は指数的にゼロに近づきます。

**実用上の影響：**
- ステップ1の入力がステップ100の出力に与える影響を学習できない
- 「最初の文で述べた主語」を「最後の文の動詞」に反映する、といったタスクが困難
- これを**長距離依存性問題（long-range dependency problem）**と呼びます`,
        },
        {
          type: "text",
          content: `下のデモで、シグナル（重要な入力）の位置と隠れ状態の関係を確認しましょう。

- **短距離**: シグナルが近い → 隠れ状態にしっかり残る
- **長距離**: シグナルが遠い → 隠れ状態から忘却される

**減衰率**を変えて、情報がどのくらい速く失われるか見てみましょう。`,
        },
        {
          type: "interactive",
          id: "long-range-demo",
        },
        {
          type: "quiz",
          data: {
            question:
              "RNNの勾配消失問題が「深いMLP」よりも深刻になりやすい理由は？",
            options: [
              "RNNの活性化関数が特殊だから",
              "同じ重み行列 W_hh が繰り返し掛け算されるため、系列が長いほど勾配が指数的に減衰するから",
              "RNNのバイアスが大きすぎるから",
              "RNNの学習率が自動的に下がるから",
            ],
            answer: 1,
            explanation:
              "MLPでは各層が異なる重み行列を持つため、勾配の挙動に多様性がありますが、RNNでは同一の W_hh が繰り返し掛けられます。W_hh の最大特異値が1未満なら、T回の掛け算で勾配は指数的にゼロに向かいます。",
          },
        },
      ],
    },
    {
      id: "lstm",
      title: "LSTM",
      blocks: [
        {
          type: "text",
          content: `**LSTM（Long Short-Term Memory）**は1997年にHochreiterとSchmidhuberが提案した、勾配消失問題を解決するアーキテクチャです。

LSTMの核心は**ゲート機構**と**セル状態（cell state）**です。通常のRNNの隠れ状態1つに対し、LSTMは2つの状態を持ちます：

- **セル状態 $C_t$** — 長期記憶。情報が「ベルトコンベア」のようにほぼそのまま流れる
- **隠れ状態 $h_t$** — 短期出力。セル状態をフィルタリングしたもの

情報の流れを制御するのが**3つのゲート**です：`,
        },
        {
          type: "text",
          content: `**1. 忘却ゲート（Forget Gate）** — 過去の記憶をどれだけ保持するか

$$f_t = \\sigma(W_f \\cdot [h_{t-1}, x_t] + b_f)$$

出力が1に近い → 記憶を保持、0に近い → 記憶を消去

**2. 入力ゲート（Input Gate）** — 新しい情報をどれだけ書き込むか

$$i_t = \\sigma(W_i \\cdot [h_{t-1}, x_t] + b_i)$$

$$\\tilde{C}_t = \\tanh(W_C \\cdot [h_{t-1}, x_t] + b_C)$$

**3. 出力ゲート（Output Gate）** — セル状態のどれだけを出力するか

$$o_t = \\sigma(W_o \\cdot [h_{t-1}, x_t] + b_o)$$

**セル状態の更新：**

$$C_t = f_t \\odot C_{t-1} + i_t \\odot \\tilde{C}_t$$

**隠れ状態の計算：**

$$h_t = o_t \\odot \\tanh(C_t)$$

ここで $\\odot$ は要素ごとの積（アダマール積）、$\\sigma$ はシグモイド関数です。`,
        },
        {
          type: "text",
          content: `**なぜLSTMが勾配消失を解決できるのか？**

セル状態 $C_t$ の更新式に注目してください：

$$C_t = f_t \\odot C_{t-1} + i_t \\odot \\tilde{C}_t$$

これは**加算**的な更新です。通常のRNNの $h_t = \\tanh(W \\cdot h_{t-1} + \\ldots)$ は行列積（**乗算**）的な更新でした。

加算的な構造により、$\\frac{\\partial C_t}{\\partial C_{t-1}} = f_t$ となり、忘却ゲートが1に近ければ**勾配がほぼそのまま伝播**します。これがResNetのスキップ接続と同じ原理で、「勾配のハイウェイ」を作っているのです。

下のデモで、LSTMセルの内部動作を確認しましょう。サンプル系列の各タイムステップで、ゲートの値とセル状態がどう変化するか見てみてください。`,
        },
        {
          type: "interactive",
          id: "lstm-cell",
        },
      ],
    },
    {
      id: "summary",
      title: "まとめ",
      blocks: [
        {
          type: "text",
          content: `このレッスンで学んだこと：

**RNN：** 隠れ状態 $h_t$ で系列の「記憶」を保持。同じ重みをすべてのタイムステップで共有する

**BPTT：** RNNを時間方向に展開し、通常の誤差逆伝播を適用。勾配は時間を遡って伝播する

**勾配消失問題：** 同一の $W_{hh}$ が繰り返し掛け算されるため、長い系列で勾配が指数的に減衰。長距離依存性を学習できない

**LSTM：** 3つのゲート（忘却・入力・出力）とセル状態により、長期記憶を保持。加算的な更新構造が勾配のハイウェイを作る`,
        },
        {
          type: "text",
          content: `LSTMは系列モデリングに革命をもたらし、機械翻訳、音声認識、テキスト生成など多くのタスクで成功を収めました。

しかし、LSTMにも限界があります。系列を**1ステップずつ順番に**処理するため、長い系列では計算が遅く、並列化が困難です。また、非常に長い系列（数千ステップ）では依然として情報の損失が起きます。

次のレッスンでは、この限界を根本的に解決する**Attention機構**を学びます。「すべてのステップを一度に見る」というアイデアが、現代のTransformerアーキテクチャ（GPT, BERTなど）の基礎になっています。`,
        },
        {
          type: "quiz",
          data: {
            question:
              "LSTMのセル状態 C_t が長距離の勾配伝播を可能にする主な理由は？",
            options: [
              "セル状態が常に正の値を取るから",
              "更新が加算的（C_t = f_t * C_{t-1} + ...）なので、忘却ゲートが1に近ければ勾配がほぼそのまま伝播するから",
              "LSTMのパラメータ数がRNNより少ないから",
              "出力ゲートが勾配を増幅するから",
            ],
            answer: 1,
            explanation:
              "通常のRNNでは h_t = tanh(W * h_{t-1} + ...) と行列積で更新するため勾配が消失しやすいですが、LSTMのセル状態は C_t = f_t * C_{t-1} + i_t * C~_t と加算的に更新されます。∂C_t/∂C_{t-1} = f_t なので、忘却ゲートが1に近ければ勾配がほぼ減衰せずに伝播します。",
          },
        },
      ],
    },
  ],
};

export default lesson8;
